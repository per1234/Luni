#ifndef FirmataDeviceLibrary_h
#define FirmataDeviceLibrary_h

// Arduino library tag file.  Provided so that FirmataDevice can be
// used as a library in the Arduino IDE, regardless of how many other include
// files may or may not be in the src directory.

#endif
